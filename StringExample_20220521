public class StringExample_20220521 {
  public static void main(String[] args) {
  
    System.out.println("Hello, World!");      // String으로 문장이기 때문에 값이 나온다.
    // System.out.println('Hello, World!');   // 이렇게 했을 경우에는 문장이 아니기 때문에 값이 출력되지 않는다.
    System.out.println('J');      // Charater ''를 쓰고 싶다면 자음, 모음처럼 문자로 표시 되어야만 값이 출력이 된다.
    //  System.oout.println("H");       // 다만 ""은 H라고 써도 문장으로 인정하기 때문에 값이 출력되긴 하다. 
    
    // * 편하게 띄어쓰기 하는 방법
     System.out.println("Hello" + "World!");    //  "Hello World!" 라고 입력한 상태여서 중간에 엔터를 치면, 이클립스가 알아서 해줌.
     
    // * 줄 바꿈을 하고 싶을 때
     Systme.out.println("Hello \nWorld!");      // 이렇게 줄바꿈 하고 싶은 공간에 '\n' 을 넣어주면 된다.
     
    // * 큰 따옴표를 표현하여 글자를 강조하기 
     System.out.println("Hello, \"World\"");    // 이렇게 강조하는 거에 \를 붙여주면 된다. 마치 html 태그처럼!
  }
}
